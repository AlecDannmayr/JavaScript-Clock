setInterval(setClock, 1000) 

function setClock() {
    const currentDate = new Date()
    const secondsRatio = curentDate.getSeconds() / 60
    const minutesRatio = curentDate.getminuets() / 60
    const hoursRatio = curentDate.gethours() / 60

}